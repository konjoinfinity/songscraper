# Secrets — never commit
.env
.env.*
!.env.example
/creds.json
/credentials.json
/token.json

# Dependencies & build output
node_modules
coverage

# OS / editor
.DS_Store

# Local scratch dir for chart review (not committed)
.charts/
